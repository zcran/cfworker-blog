---
title: "leetcode-模拟83"
date: 2026-08-08T11:31:13+08:00
tags: ["leetcode", "模拟"]
draft: false
---


## 统计桌面上的不同数字

给你一个正整数 n ，开始时，它放在桌面上。在 109 天内，每天都要执行下述步骤：

· 对于出现在桌面上的每个数字 x ，找出符合 1 <= i <= n 且满足 x % i == 1 的所有数字 i 。
· 然后，将这些数字放在桌面上。

返回在 10^9 天之后，出现在桌面上的 不同 整数的数目。

注意：

· 一旦数字放在桌面上，则会一直保留直到结束。
· % 表示取余运算。例如，14 % 3 等于 2 。


```
impl Solution {
   /// 数学推导：数字 x 会把满足 x % i == 1 的 i 放到桌面，
   /// 即 i 整除 x-1。因此：
   /// · 若 k+1 已在桌面，则 k 必被放入（因为 (k+1) % k = 1）
   /// · 从 n 开始链式传播：n → n-1 → n-2 → ... → 2
   /// · 1 永远不会被放入（任何数 % 1 = 0 ≠ 1）
   /// · 10^9 天足够传播完毕
   ///
   /// 故 n ≥ 3 时桌面为 {2,3,...,n}，共 n-1 个；
   /// n ≤ 2 时只有初始数字本身。
   pub fn distinct_integers(n: i32) -> i32 {
       if n <= 2 { 1 } else { n - 1 }
   }
}
```
