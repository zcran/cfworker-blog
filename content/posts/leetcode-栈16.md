---
title: "leetcode-栈16"
date: 2026-07-24T10:16:58+08:00
tags: ["leetcode", "栈"]
draft: false
---


## 有效的括号字符串

给你一个只包含三种字符的字符串，支持的字符类型分别是 '('、')' 和 '*'。请你检验这个字符串是否为有效字符串，如果是 有效 字符串返回 true 。

有效 字符串符合如下规则：

任何左括号 '(' 必须有相应的右括号 ')'。
任何右括号 ')' 必须有相应的左括号 '(' 。
左括号 '(' 必须在对应的右括号之前 ')'。
'*' 可以被视为单个右括号 ')' ，或单个左括号 '(' ，或一个空字符串 ""。


```
// 有效括号字符串验证（支持 '*' 通配符）
// 使用双计数器法：维护左括号数量的可能范围 [min, max]
impl Solution {
    pub fn check_valid_string(s: String) -> bool {
        let mut min = 0; // 左括号数量的最小值（将 '*' 视为空或右括号）
        let mut max = 0; // 左括号数量的最大值（将 '*' 视为左括号）

        for ch in s.chars() {
            match ch {
                '(' => {
                    min += 1;
                    max += 1;
                }
                ')' => {
                    if min > 0 {
                        min -= 1;
                    }
                    max -= 1;
                }
                '*' => {
                    if min > 0 {
                        min -= 1; // '*' 作为右括号
                    }
                    max += 1; // '*' 作为左括号
                }
                _ => unreachable!(),
            }

            // 如果 max < 0，说明右括号过多，无效
            if max < 0 {
                return false;
            }
        }

        // 左括号数量可以为 0
        min == 0
    }
}
```
